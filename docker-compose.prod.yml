# Production override — image Docker Hub (satu app monolith)
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
services:
  app:
    image: ardianryan/cms-scholargate-app:latest
    build: !reset null
